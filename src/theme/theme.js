import colors from '../utils/colors'

export const lightTheme = {
  body: colors.white,
  fontColor: colors.dark,
  boxShadow: colors.lightShadow,
}

export const darkTheme = {
  body: colors.dark,
  fontColor: colors.white,
  boxShadow: colors.darkShadow,
}